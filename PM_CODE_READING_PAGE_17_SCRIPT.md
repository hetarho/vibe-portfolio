# PM을 위한 코드 읽기 — 17페이지 진행 스크립트

> 대상 화면: `PM을 위한 코드 읽기` 17페이지 (`?s=17`, R16)  
> 화면 제목: **이제 진짜 코드를 열어요**  
> 권장 시간: 3분. 이어지는 18페이지 실습은 15분.  
> 링크·코드 최종 확인: 2026-08-24

## 이 화면의 목표

학생이 세 파일을 모두 이해하게 만드는 화면이 아니다. 실제 GitHub 코드도 앞에서 연습한 것처럼
`이름 → 입력 → 조건 → 결과` 순서로 읽을 수 있다는 사실을 보여주고, 오늘 읽을 파일 하나만 고른다.

오늘의 기본 선택은 **Cal.com의 `weekstart.ts`**다. 23줄이라 화면에서 길을 잃을 가능성이 가장 낮다.
나머지 두 파일은 난이도별 다음 연습거리로 소개만 하고 열지 않는다.

## 수업 10분 전 준비

- [ ] 브라우저 첫 번째 탭에 강의 17페이지를 연다.
- [ ] 아래의 `weekstart.ts`가 열리고 23줄인지 확인한다.
- [ ] 고정 diff가 열리고 `kiB → KiB` 한 줄만 바뀌는지 확인한다.
- [ ] GitHub 글자가 작으면 미리 브라우저 확대를 125~150%로 맞춘다.
- [ ] 학생에게는 GitHub 로그인이나 저장소 복제를 시키지 않는다. 읽기만 하면 된다.
- [ ] Cal.com의 최신 PR을 수업 중 즉석에서 고르지 않는다. 내용과 크기가 계속 바뀐다.

미리 열어둘 주소:

- 오늘 읽을 파일: <https://github.com/calcom/cal.diy/blob/main/packages/lib/weekstart.ts>
- 파일이 안 열릴 때 쓸 Raw 화면: <https://raw.githubusercontent.com/calcom/cal.diy/main/packages/lib/weekstart.ts>
- 나중에 쓸 고정 diff: <https://github.com/sindresorhus/pretty-bytes/commit/246a046>

브라우저 탭은 다음 순서로 고정한다.

1. 강의 화면
2. `weekstart.ts`
3. 고정 diff `246a046`

강의 화면을 첫 번째 탭으로 두면 GitHub에서 `⌘1`을 눌러 바로 돌아올 수 있다.

## 실제 진행 대본

### 1. 전환 — 20초

화면만 보여주고 말한다.

> 지금까지는 제가 읽기 좋게 만든 배송비 코드였어요. 이제부터는 실제 서비스와 실제
> 오픈소스의 코드를 엽니다. 화면이 갑자기 복잡해져도 읽는 순서는 똑같아요.

반드시 덧붙인다.

> 오늘 목표는 코드를 전부 이해하는 게 아니에요. 이 파일이 어떤 제품 결정을 내리는지
> 한 문장으로 말하면 성공입니다.

### 2. 세 카드 소개 — 40초

왼쪽부터 가리키며 말한다.

> 세 파일은 난이도 순서예요. 첫 번째는 23줄짜리 주 시작일, 두 번째는 59줄짜리 시간 표시,
> 세 번째는 174줄짜리 파일 크기 표시 코드예요. 세 개를 다 열지 않고, 오늘은 가장 짧은
> `weekstart.ts` 하나만 읽겠습니다.

여기서 피할 말:

- “짧으니까 쉽습니다.” → 짧아도 모르는 문법은 있을 수 있다.
- “이 코드는 별거 없어요.” → 학생이 막혔을 때 위축된다.
- “세 파일을 한번 훑어볼게요.” → 탭만 늘어나고 실습 시간이 사라진다.

### 3. 첫 번째 함수 열기 — 30초

첫 번째 카드의 **함수 열기**만 누른다. 새 탭에서 GitHub가 열리면 상단 경로와 줄 수만 가리킨다.

> 위쪽 경로를 보면 `packages / lib / weekstart.ts`예요. 지금은 코드 내용보다 파일 이름부터
> 봅니다. `weekstart`니까 한 주를 어느 요일부터 시작할지 정하는 파일이라고 먼저 추측할 수 있어요.

학생에게 질문한다.

> 미국 달력과 한국 달력에서 한 주의 첫날이 항상 같을까요?

학생의 답이 무엇이든 다음처럼 받는다.

> 맞아요. 정답을 맞히려는 질문이 아니라, 이 코드가 사용자에게 어떤 차이를 만들 수 있는지
> 먼저 떠올리는 질문이에요.

### 4. 다음 페이지와의 연결 — 40초

이 부분을 빼먹으면 “README부터 보라면서 왜 파일로 바로 왔지?”라는 혼란이 생긴다.

> 다음 화면에는 README부터 시작하는 다섯 단계가 나옵니다. 그게 혼자 레포를 탐색할 때의
> 전체 순서예요. 오늘 첫 실습은 15분뿐이라 제가 1~4단계를 대신 좁혀서 이 파일까지 데려왔고,
> 우리는 마지막 단계인 ‘함수 하나 읽기’에 시간을 쓰겠습니다.

GitHub 탭은 닫지 말고 `⌘1`로 강의 화면에 돌아온다. 오른쪽 화살표를 한 번 눌러 18페이지로 간다.

> 방금 연 파일은 그대로 두세요. 이제 다섯 단계 지도를 본 다음, 실제 함수에 적용해 볼게요.

## 학생 실습 중 막혔을 때만 보는 답안

먼저 답을 설명하지 않는다. 학생이 1분 이상 멈췄을 때 아래 질문을 한 번에 하나씩 준다.

1. 5번 줄의 함수 이름은 무엇인가?
2. 괄호 안에서 들어오는 값은 무엇이며, 값이 없으면 무엇을 쓰는가?
3. 6번 줄에는 어떤 후보들이 나열되어 있는가?
4. 7번 줄의 `indexOf`가 찾지 못하면 어떤 값이 나오는가?
5. 8번 줄은 찾지 못한 경우 어떤 숫자를 돌려주는가?

강사용 정답:

- 함수 이름: `weekStartNum`
- 입력: `weekName`
- 입력이 없을 때 기본값: `Sunday`
- `weekDays.indexOf(weekName)`은 요일의 위치를 찾는다.
- 찾으면 `0~6`, 못 찾으면 `-1`이 나온다.
- 8번 줄은 `-1`일 때 `0`을 돌려준다. `0`은 Sunday다.
- 따라서 이 함수의 한 문장 요약은 다음과 같다.

> 받은 요일을 일요일부터 시작하는 숫자 `0~6`으로 바꾸고, 알 수 없는 값이면 일요일 `0`을 사용한다.

제품 질문은 하나만 던진다.

> 알 수 없는 값이 들어왔을 때 오류를 보여주지 않고 일요일로 처리하는 것이 우리 제품 정책과 맞을까요?

두 번째 함수까지 간 학생에게만 추가로 묻는다.

> 19번 줄에서 요일을 찾지 못하면 `-1`을 돌려줍니다. 21번 줄의 정렬 계산에 `-1`이 들어가면,
> 알 수 없는 요일이 목록의 앞과 뒤 중 어디로 갈까요?

정답: 정상 요일은 `0` 이상이므로 `-1`인 항목이 앞쪽으로 갈 가능성이 크다.

## 아래 diff 영역을 설명하는 대본

17페이지 아래쪽의 링크는 이 화면에서 누르지 않고 위치만 알려준다.

> 파일은 현재 상태를 읽는 곳이고, diff는 바뀐 부분만 읽는 곳이에요. 아래 링크는 앞으로
> 작은 변경을 고르는 입구입니다. 다만 수업 중에 최신 목록에서 즉석으로 고르면 너무 큰 변경이
> 나올 수 있어서, 오늘은 제가 미리 고른 한 줄짜리 변경을 사용하겠습니다.

고정 diff `246a046`의 강사용 답:

- 파일: `index.js`
- 변경: `kiB`를 `KiB`로 수정
- 변경량: 1개 파일, 1줄 삭제, 1줄 추가
- 무엇이: 이진 바이트 단위의 대소문자 표기를 바꿈
- 왜: 사용자에게 표시되는 단위 표기를 바로잡기 위해서
- 위험은: 화면 문자열이나 문자열을 그대로 비교하는 테스트가 달라질 수 있음

학생에게 보여줄 3문장 예시:

> `index.js`의 이진 단위 목록에서 `kiB`가 `KiB`로 바뀌었어요. 사용자에게 보이는 단위의
> 대소문자를 바로잡는 변경이에요. 이 문자열을 그대로 비교하는 화면이나 테스트가 있다면 함께
> 바뀌었는지 확인해야 해요.

## 돌발 상황 복구 멘트

### GitHub가 느리거나 링크가 안 열릴 때

20초 이상 새로고침하지 않는다. Raw 주소를 열고 말한다.

> GitHub의 주변 메뉴는 읽기에 필요하지 않아서 코드만 보이는 화면으로 바꿀게요.

Raw 화면도 안 열리면 강의의 8페이지 함수 또는 12페이지 diff로 돌아간다.

> 네트워크는 오늘의 학습 목표가 아니니, 같은 읽기 순서를 아까 코드에 적용하겠습니다.

### 학생이 import나 TypeScript 문법부터 물을 때

> 좋은 질문이에요. 다만 지금은 문법을 해결하는 시간이 아니라 제품 결정을 찾는 시간이라,
> 모르는 줄에는 표시만 하고 `function`, 입력값, `return`부터 보겠습니다.

### 학생이 “하나도 모르겠어요”라고 할 때

> 전체를 이해하려고 해서 그래요. 지금은 5번 줄의 함수 이름과 8번 줄의 결과만 찾아봅시다.

### 학생이 세 파일 중 다른 것을 고르고 싶어 할 때

> 오늘은 읽는 방법을 익히기 위해 가장 짧은 파일로 같이 시작하고, 마지막에 시간이 남으면
> 원하는 파일을 두 번째로 열어볼게요.

### 실수로 큰 PR을 열었을 때

> 실제 현업에서도 먼저 변경 크기를 보고 읽을지 결정합니다. 이 PR은 오늘 연습 범위보다 크니
> 닫고, 준비한 한 줄 diff로 바꾸겠습니다.

## 절대 하지 않을 것

- 세 파일을 모두 열어 탭 사이를 오가지 않는다.
- 학생 앞에서 최신 PR 목록을 스크롤하며 실습 대상을 찾지 않는다.
- 23줄을 강사가 처음부터 끝까지 번역해 주지 않는다.
- `import`, 타입 표기, 세미콜론 같은 문법 설명으로 옆길을 새지 않는다.
- GitHub에서 Edit, Fork, Codespaces를 누르지 않는다.
- 링크 문제를 20초 넘게 해결하려고 하지 않는다.

## 이 화면을 끝내는 기준

아래 세 가지가 되면 바로 18페이지로 넘어간다.

- 학생이 오늘 읽을 파일이 `weekstart.ts`라고 안다.
- 파일 이름만 보고 “한 주의 시작 요일을 정하는 코드”라고 추측해 봤다.
- GitHub 탭을 열어 둔 채 강의 화면으로 돌아왔다.

